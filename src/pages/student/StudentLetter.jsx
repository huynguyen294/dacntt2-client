import { ModuleLayout } from "@/layouts";

const StudentLetter = () => {
  return (
    <ModuleLayout breadcrumbItems={[{ label: "Nộp đơn" }]}>
      <div className="px-2 sm:px-10">Nộp đơn</div>
    </ModuleLayout>
  );
};

export default StudentLetter;
